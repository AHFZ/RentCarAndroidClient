<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LoginTheme" parent="AppTheme">
    </style>

    <style name="RoundedColoredButton" parent="@style/Widget.AppCompat.Button.Colored">
        <item name="android:background">@drawable/rounded_colored</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="NormalTextStyle">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="NormalWhiteTextView" parent="NormalTextStyle">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="NormalColoredTextStyle" parent="NormalTextStyle">
        <item name="android:textColor">?colorAccent</item>
    </style>
</resources>
